#include <iostream>

int main()
{
	std::cout << "Ingresa un numero: ";
	int x{};
	std::cin >> x;
	
	if (x >= 10) // if exterior
	{
		if (X <= 20} // if interior
			std::cout << x << "entra entre 10 y 20";
		else 
			std::cout << x << "es mayor que 20\n";
	}
	return 0;
}