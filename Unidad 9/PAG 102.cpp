int *iPtr {}; // un puntero a un valor entero
double *dPtr {}; // un puntero a un valor double

int* iPtr2 {}; // tambien sintaxis valida (aceptable, pero no favorecida)
int * iPtr3 {}; // tambien sintaxis valida (pero no hasgas esto, parece una multiplicacion)

int *iPtr4 {}, *iPtr5 {}; // declara dos punteros a variables enteras (no recomendado)